# Project Profile: {{ project.name }}

**Generated:** {{ generated_date }}

---

## 1. Project Identity

| Field | Details |
|-------|---------|
| **Project Name** | {{ project.name }} |
| **Developer** | {{ project.developer_name | default("N/A") }} |
| **Location** | {{ project.district_name }}, {{ project.city_name }} |
| **Type** | {{ project.project_type | default("N/A") }} |
| **Status** | {{ project.status | default("N/A") }} |
| **Total Units** | {{ project.total_units | default("N/A") }} |
| **Launch Date** | {{ project.launch_date | default("N/A") }} |
| **Completion Date** | {{ project.completion_date | default("N/A") }} |
| **Grade** | {{ project.grade_primary | default("N/A") }} ({{ project.segment | default("") }}) |

---

## 2. Pricing

| Metric | Value |
|--------|-------|
| **Price (USD/m2)** | ${{ project.price_usd | default("N/A") }} |
| **Price (VND/m2)** | {{ project.price_vnd | default("N/A") }} |
| **Grade Range** | ${{ project.grade_min | default("?") }} - ${{ project.grade_max | default("?") }}/m2 |
| **Position in Grade** | {{ project.grade_position | default("N/A") }} |

{% if price_history -%}
### Price History
| Period | Price (USD/m2) | Change |
|--------|----------------|--------|
{% for h in price_history -%}
| {{ h.period }} | ${{ h.price | int }} | {{ h.change | default("—") }} |
{% endfor %}
{% endif %}

---

## 3. Developer Context

{% if developer -%}
| Field | Details |
|-------|---------|
| **Name** | {{ developer.name_en }} |
| **Stock Code** | {{ developer.stock_code | default("Private") }} |
| **HQ** | {{ developer.hq_city.name_en if developer.hq_city else "N/A" }} |
| **Established** | {{ developer.established_year | default("N/A") }} |

### Other Projects by {{ developer.name_en }}
{% for p in developer_projects -%}
- {{ p.name }} ({{ p.district }}) — {{ p.grade | default("N/A") }}, ${{ p.price | default("N/A") }}/m2
{% endfor %}
{% else -%}
*Developer information not available.*
{% endif %}

---

## 4. Location Context

**District:** {{ project.district_name }}
**District Type:** {{ project.district_type | default("N/A") }}
**District Avg. Price:** ${{ district_avg_price | default("N/A") }}/m2

### Other Projects in {{ project.district_name }}
{% for p in district_projects -%}
- {{ p.name }} — {{ p.grade | default("N/A") }}, ${{ p.price | default("N/A") }}/m2
{% endfor %}

---

## 5. Grade Peers

Projects in the same grade ({{ project.grade_primary }}):
{% for p in grade_peers -%}
- {{ p.name }} ({{ p.district }}) — ${{ p.price | default("N/A") }}/m2
{% endfor %}

---

*Report generated by MR-System*
