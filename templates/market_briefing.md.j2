# {{ city_name }} Market Briefing — {{ period }}

**Generated:** {{ generated_date }}
**Data Source:** MR-System Database ({{ project_count }} projects tracked)

---

## 1. Market Snapshot

| Metric | Value |
|--------|-------|
| Total Projects | {{ project_count }} |
| Active Selling | {{ active_selling }} |
| Under Construction | {{ under_construction }} |
| Avg. Price (USD/m2) | ${{ avg_price_usd | round(0) | int }} |
| Avg. Absorption Rate | {{ avg_absorption | round(1) }}% |

---

## 2. Grade Distribution

| Grade | Segment | Price Range (USD/m2) | Projects | Avg. Price |
|-------|---------|---------------------|----------|------------|
{% for grade in grades -%}
| {{ grade.code }} | {{ grade.segment }} | ${{ grade.min_price | int }}-${{ grade.max_price | int }} | {{ grade.project_count }} | ${{ grade.avg_price | round(0) | int }} |
{% endfor %}

---

## 3. District Highlights

### Top Districts by Average Price
| Rank | District | Avg. Price (USD/m2) | Projects |
|------|----------|---------------------|----------|
{% for d in top_districts_by_price -%}
| {{ loop.index }} | {{ d.name }} | ${{ d.avg_price | round(0) | int }} | {{ d.project_count }} |
{% endfor %}

### Top Districts by New Supply
| Rank | District | New Supply (units) | Absorption |
|------|----------|--------------------|------------|
{% for d in top_districts_by_supply -%}
| {{ loop.index }} | {{ d.name }} | {{ d.new_supply | int }} | {{ d.absorption | round(1) }}% |
{% endfor %}

---

## 4. Price Trends

{% if price_changes -%}
### Notable Price Movements
| Project | Previous | Current | Change |
|---------|----------|---------|--------|
{% for p in price_changes -%}
| {{ p.name }} | ${{ p.prev_price | int }} | ${{ p.curr_price | int }} | {{ p.change_pct | round(1) }}% |
{% endfor %}
{% else -%}
*Single period data — trend analysis requires multiple periods.*
{% endif %}

---

## 5. Supply Pipeline

| Status | Projects | Total Units |
|--------|----------|-------------|
{% for s in supply_pipeline -%}
| {{ s.status }} | {{ s.count }} | {{ s.units | int }} |
{% endfor %}

---

## 6. Key Takeaways

{% for takeaway in takeaways -%}
- {{ takeaway }}
{% endfor %}

---

*Report generated by MR-System Market Analyzer*
