# Competitive Analysis: {{ projects | map(attribute='name') | join(' vs ') }}

**Generated:** {{ generated_date }}
**Methodology:** NHO-PD 11-Dimension Comparison Framework

---

## 1. Project Overview

| Attribute | {% for p in projects %}{{ p.name }} | {% endfor %}
|-----------|{% for p in projects %}-----------|{% endfor %}
| Location | {% for p in projects %}{{ p.district }}, {{ p.city }} | {% endfor %}
| Developer | {% for p in projects %}{{ p.developer | default("N/A") }} | {% endfor %}
| Type | {% for p in projects %}{{ p.project_type | default("N/A") }} | {% endfor %}
| Units | {% for p in projects %}{{ p.total_units | default("N/A") }} | {% endfor %}
| Price (USD/m2) | {% for p in projects %}${{ p.price | default("N/A") }} | {% endfor %}
| Grade | {% for p in projects %}{{ p.grade | default("N/A") }} | {% endfor %}
| Status | {% for p in projects %}{{ p.status | default("N/A") }} | {% endfor %}

---

## 2. 11-Dimension Scoring Matrix

| Dimension | {% for p in projects %}{{ p.name }} | {% endfor %}Winner |
|-----------|{% for p in projects %}:---------:|{% endfor %}--------|
{% for dim in dimensions -%}
| {{ dim.name }} | {% for score in dim.scores %}{{ score }} | {% endfor %}{{ dim.winner }} |
{% endfor %}
| **Total** | {% for p in projects %}**{{ p.total_score }}** | {% endfor %}**{{ overall_winner }}** |

---

## 3. Dimension Analysis

{% for dim in dimensions -%}
### {{ dim.name }}
{{ dim.analysis }}

{% endfor %}

---

## 4. Strengths & Weaknesses

{% for p in projects -%}
### {{ p.name }}

**Strengths:**
{% for s in p.strengths -%}
- {{ s.dimension }} ({{ s.score }}/10): {{ s.note }}
{% endfor %}

**Weaknesses:**
{% for w in p.weaknesses -%}
- {{ w.dimension }} ({{ w.score }}/10): {{ w.note }}
{% endfor %}

{% endfor %}

---

## 5. Price-Value Assessment

{% for p in projects -%}
- **{{ p.name }}**: ${{ p.price | default("N/A") }}/m2, Score: {{ p.total_score }}/110 — {{ p.value_assessment }}
{% endfor %}

---

## 6. Recommendation

{% for rec in recommendations -%}
- **{{ rec.buyer_type }}**: {{ rec.project }} — {{ rec.reason }}
{% endfor %}

---

*Report generated by MR-System Competitor Benchmarker*
